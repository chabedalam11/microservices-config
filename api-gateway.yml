server:
  port: 8080

spring:
  application:
    name: api-gateway

  cloud:
    gateway:
      routes:
        - id: discovery
          uri: http://localhost:8761
          predicates:
            - Path=/discovery,/discovery/**
          filters:
            - StripPrefix=1
